openapi: 3.0.0
info:
  title: User Service & Gamey Bot API
  version: 1.0.0
  description: API for managing users with MongoDB and bot movement selection for YEN (YOVI).

servers:
  - url: http://localhost:4000
    description: Development server
  - url: http://20.199.40.219
    description: Production server

paths:
  /createuser:
    post:
      summary: Creates a new user
      description: Stores a new user in MongoDB Atlas.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - username
                - password
                - confirmPassword
              properties:
                username:
                  type: string
                  example: "testuser"
                password:
                  type: string
                  example: "mysecurepass"
                confirmPassword:
                  type: string
                  example: "mysecurepass"
      responses:
        '201':
          description: User created successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  username:
                    type: string
        '400':
          description: Validation error (missing fields or password mismatch)
        '409':
          description: Conflict (Username already exists)

  /play:
    get:
      summary: Requests the next move given the current board state in YEN notation.
      parameters:
        - name: position
          in: query
          required: true
          description: Current board state in YEN notation (JSON format without quotes on keys).
          schema:
            type: string
            example: '{size:3,turn:0,"players":["B","R"],"layout":"./../..."}'
        - name: bot_id
          in: query
          required: false
          description: Identifier of the bot that should choose the move.
          schema:
            type: string
            default: montecarlo_bot
        - name: api_version
          in: query
          required: false
          description: API version to use.
          schema:
            type: string
            default: v1
      responses:
        '200':
          description: Next move coordinates calculated successfully.
          content:
            application/json:
              schema:
                type: object
                properties:
                  coords:
                    type: object
                    description: The coordinates where the bot chooses to place its piece.
                    properties:
                      x:
                        type: integer
                        description: X coordinate (distance from side A).
                        example: 1
                      y:
                        type: integer
                        description: Y coordinate (distance from side B).
                        example: 1
                      z:
                        type: integer
                        description: Z coordinate (distance from side C).
                        example: 0
        '400':
          description: Error in parameter format or invalid position.
          content:
            application/json:
              schema:
                type: object
                properties:
                  api_version:
                    type: string
                  bot_id:
                    type: string
                  message:
                    type: string
                    description: Description of the error.
        '500':
          description: Error contacting the Rust module or processing the response.
          content:
            application/json:
              schema:
                type: object
                properties:
                  api_version:
                    type: string
                  bot_id:
                    type: string
                  message:
                    type: string
                    description: Description of the error.